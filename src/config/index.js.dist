const raven = {
  dsn: '',
};

const testMode = true;

const getApi = () => ({
  scheme: '',
  host: '',
  routes: {},
});

const authentication = {
  apiKey: '',
};
localStorage.setItem('x-auth-token', authentication.apiKey);

export default {
  getApi,
  authentication,
  raven,
  testMode
};
